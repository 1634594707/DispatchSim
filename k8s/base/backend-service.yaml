apiVersion: v1
kind: Service
metadata:
  name: dispatchsim-backend
  namespace: dispatchsim
spec:
  selector:
    app: dispatchsim-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
